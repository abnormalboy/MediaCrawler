# 更新记录

## 2026-08-15

### 优化：拉评论 / 拉详情间隔加长到 10 秒

- 新增 `CRAWLER_COMMENT_SLEEP_SEC`（默认 10 秒），命令行 `--comment_sleep` 可再拉长。
- `CRAWLER_MAX_SLEEP_SEC` 从 2 秒改为 10 秒（搜索翻页、拉笔记详情）。
- 降低连续请求被踢登录的概率。

### 新增：小红书 App ADB 标准流程

- 用已登录的官方 App 搜索、打开笔记、提取分享链接，不再走 Chrome CDP / Web 接口。
- 标准编排：`XhsAdbPipeline`（`tools/adb_xhs/pipeline.py`），整次任务只发 **1 次**搜索 Intent。
- 看完一条只返回搜索列表，再点下一条；短链粘到评论框读取，避免污染顶栏搜索。
- 入口：
  - `uv run python tools/xhs_adb.py search -k 吧唧 -p 1 --open 5`
  - `./scripts/xhs_adb_search.sh 吧唧 5 1`
- 说明文档：`docs/ADB小红书标准流程.md`
- 结果写入 `data/xhs/adb/`（jsonl + markdown 报告）

### 优化

- 连接已有 Chrome 时，CDP 清理不再关闭用户浏览器标签页（`tools/cdp_browser.py`）。
